# Example .env for Garage
# Copy this file to .env and replace placeholders with secure values.
# Generate RPC secret (hex, 32 bytes): openssl rand -hex 32
# Generate tokens (base64, 32 bytes): openssl rand -base64 32

# RPC secret used by config/garage.toml -> rpc_secret
RPC_SECRET=CHANGE_ME_RPC_SECRET_32_HEX

# Admin API token -> admin.admin_token
ADMIN_TOKEN=CHANGE_ME_ADMIN_TOKEN_BASE64

# Metrics token -> admin.metrics_token
METRICS_TOKEN=CHANGE_ME_METRICS_TOKEN_BASE64